# pomodoro
Simple Pomodoro Timer App

# Requirements for Linux

Libraries needed for playing audio (https://pub.dev/packages/audioplayers)
```bash
sudo apt-get install clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev
```